#lang info

(define test-responsibles '(("options.rkt" (robby chrdimo))
                            ("logging.rkt" stamourv)
                            ("list.rkt" jay)
                            ("match.rkt" samth)))
